# ============================================================
# Siestai — Environment Variables
# Copy to .env.local and fill in real values
# ============================================================

# --- Database (packages/db) ---
DATABASE_URL=postgresql://postgres:postgres@localhost:5433/siestai
POSTGRES_PASSWORD=postgres

# --- LiveKit ---
# Set to "local" to use local LiveKit server (docker-compose.dev.yml), or "cloud" for LiveKit Cloud
LIVEKIT_ENVIRONMENT=local
# Cloud credentials (only needed when LIVEKIT_ENVIRONMENT=cloud)
LIVEKIT_URL=wss://your-project.livekit.cloud
LIVEKIT_API_KEY=your_api_key
LIVEKIT_API_SECRET=your_api_secret

# --- API (apps/api) ---
PORT=4200
BACKEND_URL=http://localhost:4200
FRONTEND_URL=http://localhost:3000
ARENA_INVITE_SECRET=your-random-32-char-secret-here

# --- Auth (Better Auth via apps/api) ---
BETTER_AUTH_URL=http://localhost:4200
BETTER_AUTH_SECRET=change-me-to-a-random-32-char-secret-value
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret
# COOKIE_DOMAIN=.siestai.com  # Only needed in staging/production

# --- Web (apps/web) ---
# When LIVEKIT_ENVIRONMENT=local, use ws://localhost:7880; for cloud, use your wss:// URL
NEXT_PUBLIC_LIVEKIT_URL=ws://localhost:7880
NEXT_PUBLIC_API_URL=http://localhost:4200

# --- AI Providers ---
OPENAI_API_KEY=your_openai_api_key
ANTHROPIC_API_KEY=your-anthropic-api-key
